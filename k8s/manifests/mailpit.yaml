apiVersion: v1
kind: Service
metadata:
  name: speakup-mailpit-svc
spec:
  selector:
    app: speakup-mailpit-pod
  ports:
    - name: smtp
      port: 1025
      targetPort: 1025
    - name: http
      port: 8025
      targetPort: 8025
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: speakup-mailpit-deployment
spec:
  selector:
    matchLabels:
      app: speakup-mailpit-pod
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: speakup-mailpit-pod
    spec:
      containers:
        - name: mailpit
          image: "axllent/mailpit:v1.31"
          ports:
            - name: smtp
              containerPort: 1025
            - name: http
              containerPort: 8025
